import { XuxaStringPipe } from './xuxa-string.pipe';

describe('XuxaStringPipe', () => {
  it('create an instance', () => {
    const pipe = new XuxaStringPipe();
    expect(pipe).toBeTruthy();
  });
});
